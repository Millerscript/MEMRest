workspace 'MEMRest'
project 'Example/MEMRest.xcodeproj'
use_frameworks!

platform :ios, '15.0'

target 'MEMRestExample' do
  pod 'MEMRest', :path => './'
  pod 'MEMBase', :git => 'git@github.com:Millerscript/MEMBase.git', :branch => 'main'
  
  target 'MEMRestTests' do
    inherit! :search_paths

    
  end
end
